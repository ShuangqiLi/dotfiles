# Copy to env.zsh (ignored by git):  cp local/env.zsh.example local/env.zsh
# Sourced at end of zshrc — use for proxy, PATH, API keys, DISPLAY, etc.

# export HTTP_PROXY=http://127.0.0.1:7890
# export HTTPS_PROXY="$HTTP_PROXY"
# export PATH="/opt/custom/bin:$PATH"
